{
  "name": "@alcmaeon/server",
  "private": true,
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --exit-child ./src/index.ts",
    "build": "rimraf ./dist && tsc",
    "lint": "eslint ./src --quiet",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@alcmaeon/shared": "workspace:^",
    "@fastify/cookie": "^8.3.0",
    "@fastify/cors": "^8.2.1",
    "@prisma/client": "^4.12.0",
    "@trpc/server": "^10.18.0",
    "dotenv": "^16.0.3",
    "fastify": "^4.15.0",
    "typescript": "^4.9.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "prisma": "^4.12.0",
    "rimraf": "^4.4.1",
    "ts-node-dev": "^2.0.0"
  }
}
